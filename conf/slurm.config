process.executor = 'slurm'

process {
    queue = 'compute'
    clusterOptions = '--constraint=cal'
    maxForks = 10
    time = '24h'
    maxRetries = 1
    errorStrategy = 'retry'
     beforeScript = '''
        module load singularity
    '''
}
