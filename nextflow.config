// nextflow.config - minimal config
process {
    executor = 'local'
    cpus = 1
    memory = '2 GB'
}

// Load additional config fragments if needed
includeConfig 'conf/slurm.config'
includeConfig 'conf/resources.config'
